function detectEvalFlakes(runs,{maxStddev=0.2}={}){const groups=groupBy(runs,r=>r.id??r.caseId);const flakes=[];for(const [id,items]of groups){const scores=items.map(i=>Number(i.score??(i.passed?1:0)));const avg=scores.reduce((a,b)=>a+b,0)/scores.length;const variance=scores.reduce((a,b)=>a+(b-avg)**2,0)/scores.length;const stddev=Math.sqrt(variance);if(stddev>maxStddev)flakes.push({id,count:items.length,stddev:round(stddev)});}return{stable:flakes.length===0,flakes};}function groupBy(items,key){const m=new Map();for(const item of items){const k=key(item);m.set(k,[...(m.get(k)??[]),item]);}return m;}function round(n){return Math.round(n*10000)/10000;}export{detectEvalFlakes};
