const BAD=/\b(ignore previous|system prompt|developer instruction|exfiltrate|jailbreak|override)\b/i;function scoreVectorPoison(doc){const text=String(doc?.text??doc??"");const reasons=[];if(BAD.test(text))reasons.push("instruction_like_payload");if((text.match(/https?:\/\//g)??[]).length>5)reasons.push("link_farm");if(text.length>20000)reasons.push("oversized_chunk");const score=Math.min(1,reasons.length/3);return{poisoned:score>=0.33,score,reasons};}function filterPoisoned(docs,{maxScore=0.33}={}){return docs.filter(doc=>scoreVectorPoison(doc).score<=maxScore);}export{scoreVectorPoison,filterPoisoned};
