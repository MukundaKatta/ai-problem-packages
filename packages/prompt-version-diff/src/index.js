function diffPrompts(before,after){const a=lines(before),b=lines(after);const added=b.filter(x=>!a.includes(x));const removed=a.filter(x=>!b.includes(x));const risky=added.filter(x=>/ignore|secret|system|tool|always|never/i.test(x));return{changed:added.length+removed.length>0,added,removed,risky};}function lines(text){return String(text??"").split(/\n+/).map(x=>x.trim()).filter(Boolean);}export{diffPrompts};
