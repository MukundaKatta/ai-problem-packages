function auditStaleness(docs,{now=Date.now(),maxAgeDays=30,currentVersion}={}){return docs.map(doc=>{const ageDays=doc.updatedAt?Math.floor((now-new Date(doc.updatedAt).getTime())/86400000):Infinity;const reasons=[];if(ageDays>maxAgeDays)reasons.push("too_old");if(currentVersion&&doc.version&&doc.version!==currentVersion)reasons.push("version_mismatch");return{id:doc.id,stale:reasons.length>0,ageDays,reasons};});}function filterFresh(docs,options){const audit=auditStaleness(docs,options);const stale=new Set(audit.filter(x=>x.stale).map(x=>x.id));return docs.filter(doc=>!stale.has(doc.id));}export{auditStaleness,filterFresh};
