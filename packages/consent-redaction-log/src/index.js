function redactWithConsent(text,{allowedTypes=[]}={}){const rules={email:/[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}/gi,phone:/\+?\d[\d\s().-]{7,}\d/g};let value=String(text??"");const log=[];for(const [type,re]of Object.entries(rules)){if(allowedTypes.includes(type))continue;value=value.replace(re,match=>{log.push({type,length:match.length});return "[REDACTED:"+type+"]";});}return{text:value,log};}function summarizeRedactions(log){return log.reduce((acc,item)=>({...acc,[item.type]:(acc[item.type]??0)+1}),{});}export{redactWithConsent,summarizeRedactions};
