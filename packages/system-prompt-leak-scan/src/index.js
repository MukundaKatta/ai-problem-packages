const LEAK=/\b(system prompt|developer message|hidden instruction|initial instructions|do not reveal|internal policy)\b/i;function scanPromptLeak(output,{secrets=[]}={}){const text=String(output??"");const findings=[];if(LEAK.test(text))findings.push({type:"prompt_leak_language"});for(const secret of secrets){if(secret&&text.includes(secret))findings.push({type:"secret_echo",secret});}return{leaked:findings.length>0,findings};}function redactPromptLeak(output,options){let text=String(output??"");for(const finding of scanPromptLeak(text,options).findings){if(finding.secret)text=text.replaceAll(finding.secret,"[REDACTED_SECRET]");}return text.replace(LEAK,"[REDACTED_PROMPT_REFERENCE]");}export{scanPromptLeak,redactPromptLeak};
