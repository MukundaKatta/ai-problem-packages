function cosine(a,b){let dot=0,aa=0,bb=0;for(let i=0;i<Math.min(a.length,b.length);i++){dot+=a[i]*b[i];aa+=a[i]*a[i];bb+=b[i]*b[i];}return aa&&bb?dot/(Math.sqrt(aa)*Math.sqrt(bb)):0;}function dedupeEmbeddings(items,{threshold=0.98}={}){const kept=[];const duplicates=[];for(const item of items){const match=kept.find(k=>cosine(k.embedding,item.embedding)>=threshold);if(match)duplicates.push({id:item.id,duplicateOf:match.id});else kept.push(item);}return{kept,duplicates};}export{cosine,dedupeEmbeddings};
